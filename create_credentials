#!/bin/bash

PASS=`openssl rand -hex 24`
SALT=`openssl rand -hex 24`

CREDENTIAL_FILE_NAME=$1

touch $CREDENTIAL_FILE_NAME
chmod 755 $CREDENTIAL_FILE_NAME

echo "export GIT_ENCRYPT_PASS_FIXED=$PASS" >> $CREDENTIAL_FILE_NAME
echo "export GIT_ENCRYPT_SALT_FIXED=$SALT" >> $CREDENTIAL_FILE_NAME
